apiVersion: v1
kind: Service
metadata:
  name: evaluation-service
  namespace: tech-challenge2
spec:
  type: ClusterIP
  selector:
    app: evaluation-service
  ports:
    - port: 80
      targetPort: 8004