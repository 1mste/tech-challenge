apiVersion: v1
kind: Service
metadata:
  name: targeting-service
  namespace: tech-challenge2
spec:
  type: ClusterIP
  selector:
    app: targeting-service
  ports:
    - port: 80
      targetPort: 8003